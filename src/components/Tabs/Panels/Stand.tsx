import { api } from "../../../utils/api";

const Stand: React.FC = ({}) => {
  const createPlayer = api.player.create.useMutation();

  return <div>Dit is de standfasdfadsfs</div>;
};

export default Stand;
